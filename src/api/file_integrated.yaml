type: file
example: "resources_test/task_cyto_batch_integration/leomazzi_cyto_spleen_subset/integrated.h5ad"
label: Integrated
summary: "Integrated dataset which batch effect was corrected by an algorithm"
info:
  format:
    type: h5ad
    layers:
      - type: double
        name: integrated
        description: The integrated data as returned by a batch correction method
        required: true
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
      - type: string
        name: method_id
        description: "A unique identifier for the method"
        required: true
      - type: object
        name: parameters
        description: "The parameters used for the integration"
        required: false
      - name: parameter_flowsom_xdim
        type: integer
        description: Parameter used to define the dimensions of the self-organizing map (SOM) grid used for flowsom clustering.
        required: true
      - name: parameter_flowsom_ydim
        type: integer
        description: Parameter used to define the dimensions of the self-organizing map (SOM) grid used for flowsom clustering.
        required: true
      - name: parameter_flowsom_nclus
        type: integer
        description: Parameter used to define the number of clusters used in the meta-clustering step of the flowsom algorithm.
        required: true 